package com.mediplus.faceverify.core.ui.theme

import androidx.compose.material3.Typography

/**
 * App typography. Uses Material 3 defaults (system font, which honors the user's dynamic font-size
 * setting for accessibility — Principle III). Centralized here so type is a shared token.
 */
val AppTypography = Typography()
