<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Self-update: stream the verified APK into a PackageInstaller session (sideloaded fleet,
         no Play). The operator still confirms each install on unmanaged devices. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Rollback backups live in shared Downloads so they survive uninstall; only pre-Q devices
         need the legacy write permission for that. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Camera + NFC drive the core verification flows, but the app degrades gracefully
         (clear message) when a device lacks either, so they are not hard-required. -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <application
        android:name=".SpApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpApp"
        tools:targetApi="34">

        <!-- singleTask, not the default: this is a single-Activity app, so a second MainActivity
             instance is never wanted. Android only reuses a task when the incoming intent matches
             the task's root intent, so a task started any way other than by tapping the launcher
             icon (`adb am start -n`, a shortcut, another app) gets a *new* MainActivity stacked on
             top at every later launcher tap. The process — and with it the in-memory session —
             survives, but the new Activity brings a new NavHost that starts at sign-in: the
             operator is dropped back to the sign-in screen mid-journey with their session still
             live. singleTask makes that unreachable by construction. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.SpApp"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Install-session status callbacks; targeted only by our own mutable PendingIntent. -->
        <receiver
            android:name=".core.update.UpdateStatusReceiver"
            android:exported="false" />

        <!-- Best-effort relaunch after a successful self-update replaces this package. -->
        <receiver
            android:name=".core.update.UpdateRelaunchReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
